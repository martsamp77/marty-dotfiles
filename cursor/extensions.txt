# Cursor Extensions Manifest
# Generated by: cursor --list-extensions
# Install all:  cat extensions.txt | grep -v '^#' | xargs -L 1 cursor --install-extension
#
# To regenerate from your current install, run:
#   cursor --list-extensions > cursor/extensions.txt
#
# Lines starting with # are comments and ignored during sync.
